# Copy this file to .env (in this folder) and paste your token after the = sign.
# Get a free token at huggingface.co → Settings → Access Tokens
# (allow "Make calls to Inference Providers").
SPICE_HUGGINGFACE_API_KEY=
